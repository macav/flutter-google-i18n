name: flutter_google_i18n
description: This package simplifies the translations of the app by using Google spreadsheet, thanks to which a non-tech person can update texts in the app.
version: 0.1.0
homepage: https://github.com/macav/flutter-google-i18n

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_cache_manager: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
